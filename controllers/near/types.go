package controllers

const (
	// EnvDataPath is the environment variable to locate data path
	EnvDataPath = "KOTAL_DATA_PATH"
	// EnvConfigPath is the environment variable to locate config path
	EnvConfigPath = "KOTAL_CONFIG_PATH"
	// EnvNetwork is the environment variable to set NEAR network
	EnvNetwork = "KOTAL_NEAR_NETWORK"
)
